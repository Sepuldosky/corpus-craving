CRAVING — ROADMAP (BRÚJULA)
Hacia dónde va el módulo y en qué orden. Qué construir next.
=====================================================
Documento vivo. Fija el RUMBO (secuencia estable de lo que sigue).
"Dónde estamos dentro de esto" lo dice docs/craving_estado.md (foto de HOY).
Leer JUNTO con craving_estado.md al decidir el próximo bloque.
=====================================================


0. CÓMO SE USA
----------------

Este doc ORDENA lo que sigue; craving_estado.md dice en qué punto estamos. El
detalle de diseño vive en Craving_Architecture.md (doc particular) — acá NO se
duplica; solo el orden y el criterio de entrada de cada tramo. Metodología:
corpus/docs/corpus_flujo_trabajo.txt (canónica del ecosistema).

NOTA DE LECTURA (cita FLU-22 y el voto del autor del 2026-07-19): este doc es
INTENCIÓN PURA, no autoridad. Los IDs que aparecen acá son CITAS a normas cuya sede
está en otro lado — este roadmap no acuña ninguna. Un "limpio" del gate de coherencia
sobre este archivo se reporta NO-AUDITABLE POR DISEÑO.


1. INMEDIATO
--------------

  [1] HECHO (2026-07-14). Verificación en juego del v1 en tres rondas: los 12
      entries del CHANGELOG quedaron [APLICADO]. Los puntos calientes se
      resolvieron todos — KeyValues del spawnmenu funcionan; las rutas CS:S
      nunca hicieron falta (el pack STALKER cubre); los sonidos se
      re-seleccionaron (entry 11). Queda opcional la pata sin-Cargo del
      checklist, diferida por el autor.

  [2] HECHO (2026-07-14). El v1 quedó commiteado y pusheado a `origin/main`
      (14 commits: docs + los 10 archivos .lua del módulo — entre ellos
      shared/corpus_craving_dev.lua, que registra el concommand
      craving_selftest; no hay ningún archivo con ese nombre).

  [3] **Es lo próximo**: negociar `ApplyExternalCondition` con Coagulant cuando
      su Block 3 cierre (hoy el puente es mock-first —cita FLU-17—, arquitectura
      §4; Coagulant §12 ya lista a Craving como consumidor — falta la vía de
      condición externa). La firma la congeló CRV-4 desde acá, o sea desde el
      CONSUMIDOR: el dueño todavía no la ratificó, y eso es la deuda D-5 del
      registro. Al negociarla se cierra. Mientras tanto rige CRV-2/CRV-3:
      degradación por CAPACIDAD, no por presencia.


2. SIGUIENTE — bloques futuros (orden por afinidad, arquitectura §14)
----------------------------------------------------------------------

  [4] Cocinar (crudo → cocido): carne de mutante ZONA, fogata o Workbench de
      Cargo (su diseño está en Workbench_Arquitectura.md, bloque futuro — CRG-50
      a CRG-54; el patrón de registro es el de siempre: Cargo pone el banco,
      Craving pone las recetas, cita CRG-1). Arrastra comida cruda/agua de río →
      enfermedad (misma vía §4) y caza/harvest. Criterio de entrada: v1
      verificado + Coagulant Block 3 en juego.

  [5] Efectos de consumibles: vodka/drunk (screen effects), buffs estilo GAMMA.
      Criterio de entrada: sistema de efectos temporales diseñado (bloque
      propio, corto).

  [6] Agua avanzada: cantimplora rellenable (ítem único con blob de Cargo),
      beber del mundo (D3), props del mapa comestibles (D2).

  [7] Consumir eventos de Coagulant (sangrado ↑ sed) — azúcar, cuando [3] cierre.

Descartados salvo pedido explícito: spoilage (rompe el modelo stackeable de
Cargo), farming (fuera del espíritu del ecosistema). Fronteras duras (cita COR-1 y
COR-10: la lógica de dominio ajena no baja acá, y nada de esto sube al framework):
stamina/sueño = Coagulant; radiación sin dueño; NPCs = Cortex.


=====================================================
Documentos complementarios:
  docs/craving_estado.md             → foto de HOY (dónde estamos)
  docs/Craving_Architecture.md       → diseño ratificado (Block 4)
  docs/Craving_Block4_Semilla.md     → registro histórico de decisiones A-F
  ../corpus/docs/corpus_flujo_trabajo.txt → cómo ejecutar cada tramo
=====================================================
